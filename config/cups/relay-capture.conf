# Root-controlled timing for the WSL physical-endpoint simulator.
# CUPS reads no arbitrary Relay environment variables. This is the only
# accepted runtime setting; the capture root and BRF geometry are fixed.
RELAY_PAGE_DELAY_SECONDS=5.0
