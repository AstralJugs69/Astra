# CUPS does not load arbitrary environment configuration for this backend.
# The installed physical-endpoint simulator uses the root-controlled source
# config/cups/relay-capture.conf, which permits only its bounded page delay.
